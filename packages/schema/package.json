{
  "name": "@phoenix-security/schema",
  "version": "0.1.0",
  "description": "Shared verdict schema and TypeScript types for Phoenix Security Blue Shield - Firewall",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "Apache-2.0",
  "files": ["dist", "schemas"],
  "scripts": { "build": "tsc", "prepublishOnly": "npm run build" },
  "devDependencies": { "typescript": "^5.4.0" }
}
